// import { getCookie } from "./Cookie";

const config = {
    api : 'http://54.180.79.156',
    token : {
      // headers: { authorization: `Bearer ${getCookie('JWT')}`}
  },
};

export { config };
